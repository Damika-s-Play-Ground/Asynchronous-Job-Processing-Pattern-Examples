# ==============================================================================
# example.env — Template for environment configuration
# ==============================================================================
# HOW TO USE:
#   1. Copy this file to .env:   cp example.env .env
#   2. Replace every placeholder value below with your real AWS credentials
#   3. Never commit the .env file (it is listed in .gitignore)
# ==============================================================================


# ── AWS Credentials ────────────────────────────────────────────────────────────
# Obtain from: AWS Console → IAM → Users → <your user> → Security credentials → Access keys
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

# ── AWS Region ─────────────────────────────────────────────────────────────────
# Free Tier is available in all standard regions. us-east-1 is recommended.
AWS_DEFAULT_REGION=us-east-1

# ── SAM Stack Name ─────────────────────────────────────────────────────────────
# The CloudFormation stack name for this deployment.
# You can keep the default or change it to something unique.
STACK_NAME=async-job-processing-demo
